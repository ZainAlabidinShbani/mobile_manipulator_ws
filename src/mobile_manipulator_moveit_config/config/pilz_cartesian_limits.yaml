# pilz_cartesian_limits.yaml
# Cartesian limits used by the Pilz industrial motion planner pipeline.
# Kept at the Setup Assistant defaults; OMPL is the default pipeline for Phase 5.
cartesian_limits:
  max_trans_vel: 1.0
  max_trans_acc: 2.25
  max_trans_dec: -5.0
  max_rot_vel: 1.57
