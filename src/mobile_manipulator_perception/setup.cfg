# Without this, colcon installs console_scripts into install/<pkg>/bin, where
# `ros2 run` cannot see them ("No executable found").  ament_python expects
# them in install/<pkg>/lib/<pkg>.
[develop]
script_dir=$base/lib/mobile_manipulator_perception
[install]
install_scripts=$base/lib/mobile_manipulator_perception
